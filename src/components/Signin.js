import React from 'react';
import SigninFormContainer from './SigninFormContainer';

const Signin = () => (
  <div>
    <h1>Signin</h1>
    <SigninFormContainer />
  </div>
);

export default Signin;
